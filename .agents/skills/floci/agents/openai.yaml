interface:
  display_name: "Floci"
  short_description: "Repo-specific patterns and workflows for floci"
  default_prompt: "Use the floci repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true